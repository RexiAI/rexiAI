name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  release:
    uses: RexiAI/my-engineering-standards/.github/workflows/ci-release.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
